# TYPE  DATABASE  USER  ADDRESS  METHOD  [OPTIONS]
host    g108crm   g108  172.18.0.3/32   scram-sha-256

# Existing rule for local connections
local   all       all   trust

# Existing rule for all hosts requiring SSL (optional, but keep it if desired)
hostssl all       all   all             scram-sha-256